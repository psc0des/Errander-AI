global:
  resolve_timeout: 5m

route:
  receiver: 'n8n-webhook'
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: 'n8n-webhook'
    webhook_configs:
      - url: 'http://host.docker.internal:5678/webhook/prometheus-alert'
        send_resolved: true